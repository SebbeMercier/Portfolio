import Hero from '../components/Hero';

const Home = () => {
    return (
        <main className="pt-[72px]"> {/* 72px = hauteur Navbar + marge */}
            <Hero />
        </main>
    );
};

export default Home;